# Inputs

`words.json` is the reviewable source for each puzzle: region name, accent
color, grid size, the word list, and the generated letter grid. Edit the
`words` array to change what's hidden in a region's puzzle, then regenerate
`index.html` (the page bakes the grid and the answer-key highlights in
statically — there's no client-side solver).
